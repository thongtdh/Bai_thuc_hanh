import math
class Circle:
   def __init(self, radius):
      """
      Khởi tạo đối tượng Circle với bán kình.
      """
      self.radius radius
   def area (self):
      """
      Tình diện tích của hình tròn.
      """
      return math.pi (self.radius 2)
if_name_ == "main":
   radius float (input("Nhập bán kinh của hình tròn: "))
   circle = Circle (radius)
   print (f"Diện tích của hình tròn có bán kình (radius) là: (circle.area():.2f}")
